window.__DS_EP=window.__DS_EP||[];window.__DS_EP[15]=".25) { sx = forendX - 4; sy = forendY - 10 - (phase / 0.25) * 4; }\n          else if (phase < 0.55) { const k = (phase - 0.25) / 0.3; sx = forendX - 10 - k * 4; sy = forendY - 2; }\n          else if (phase < 0.8) { const k = (phase - 0.55) / 0.25; sx = forendX - 4; sy = forendY - 2 + k * 2; }\n          else { sx = forendX; sy = forendY; }\n          ctx.strokeStyle = skin;\n          ctx.lineWidth = 5;\n          ctx.beginPath();\n          ctx.moveTo(handX + 6, 1);\n          ctx.lineTo(sx, sy);\n          ctx.stroke();\n          ctx.fillStyle = skin;\n          rr(ctx, sx - 2, sy - 3, 11, 8, 3);\n          if (phase > 0.25 && phase < 0.75) {\n            ctx.fillStyle = \"#3a3020\";\n            rr(ctx, sx + 8, sy, 18, 4, 1);\n          }\n        } else if (anim === \"charge\") {\n          const k = Math.sin(rt * Math.PI);\n          const sx = forendX - 4;\n          const sy = forendY - 2 - k * 6;\n          ctx.strokeStyle = skin;\n          ctx.lineWidth = 5;\n          ctx.beginPath();\n          ctx.moveTo(handX + 6, 1);\n          ctx.lineTo(sx, sy);\n          ctx.stroke();\n          ctx.fillStyle = skin;\n          rr(ctx, sx - 2, sy - 3, 11, 8, 3);\n        } else {\n          ctx.fillStyle = skin;\n          rr(ctx, forendX - 2, forendY - 3.5, 12, 8, 3);\n        }\n      } else if (twoH) {\n        // steady support palm under forend \u2014 palm only, thin wrist line\n        ctx.strokeStyle = skin;\n        ctx.lineWidth = 5;\n        ctx.lineCap = \"round\";\n        ctx.beginPath();\n        ctx.moveTo(handX + 6, 1);\n        ctx.lineTo(forendX, forendY);\n        ctx.stroke();\n        ctx.fillStyle = skin;\n        rr(ctx, forendX - 2, forendY - 3.5, 12, 8, 3);\n      }\n    }\n\n    ctx.restore();\n    ctx.restore();\n  }\n\n  function drawEnemy(ctx, e) {\n    // Side profile only \u2014 horizontal flip, never face-on\n    if (e.dead) {\n      // fallen forward onto the ground\n      const fall = Math.min(1, (e.fallT || 0));\n      const ang = (e.facing >= 0 ? 1 : -1) * (Math.PI / 2) * fall;\n  ";
