window.__DS_EP=window.__DS_EP||[];window.__DS_EP[27]="gh\", String(this.high));\n      this.say(\"SIGNAL LOST\", 3);\n    }\n    kill(e) {\n      if (e.dead) return;\n      e.dead = true;\n      e.fallT = 0.01;\n      e.corpseLife = 8;\n      this.score += e.score;\n      this.credits += e.score;\n      this.sfx.hit();\n      this.burst(e.x, e.y, e.color, 16, 190);\n      this.burst(e.x, e.y - 10, \"#8a1020\", 12, 160);\n      if (Math.random() < 0.16) {\n        const w = this.weapon();\n        w.reserve += w.id === \"pistol\" ? 6 : 3;\n        this.say(\"+ AMMO\", 0.35);\n      }\n    }\n    burst(x, y, color, n, p) {\n      for (let i = 0; i < n; i++) {\n        this.particles.push({\n          x,\n          y,\n          vx: rand(-p, p),\n          vy: rand(-p, p),\n          life: rand(0.18, 0.52),\n          max: 0.52,\n          size: rand(2, 5),\n          color\n        });\n      }\n    }\n    say(t, s) {\n      this.msg = t;\n      this.msgT = s;\n    }\n    nearShop() {\n      const shop = this.doors[0];\n      return Math.abs(this.player.x - shop.x) < 70 && this.phase === \"intermission\";\n    }\n    toggleShop() {\n      if (this.phase === \"intermission\") {\n        this.shopOpen = !this.shopOpen;\n        this.onShop(this.shopOpen);\n        if (this.shopOpen) this.say(\"ARMORY\", 0.4);\n        return;\n      }\n      if (this.phase === \"combat\" && this.nearShop()) {\n        this.shopOpen = !this.shopOpen;\n        this.onShop(this.shopOpen);\n      }\n    }\n    closeShop() {\n      this.shopOpen = false;\n      this.onShop(false);\n    }\n    continueWaves() {\n      this.closeShop();\n      this.beginWave();\n    }\n    buy(item) {\n      if (this.credits < item.cost) return \"Not enough credits.\";\n      if (item.kind === \"patch\") {\n        if (this.player.hp >= this.player.max) return \"Vitals already sealed.\";\n        this.credits -= item.cost;\n        this.player.hp = clamp(\n          this.player.hp + (item.id === \"kit\" ? this.player.max : 40),\n          0,\n          this.player.max\n        );\n        this.sfx.buy();\n        return \"Patch applied.\";\n     ";
