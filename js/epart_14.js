window.__DS_EP=window.__DS_EP||[];window.__DS_EP[14]="           const k = (rt - 0.48) / 0.30;\n            mx = handX + 1; my = 22 - k * 20;\n          } else {\n            const k = (rt - 0.78) / 0.22;\n            mx = handX + 1; my = 2 + (1 - k) * 3;\n            showMag = k < 0.85;\n          }\n          // thin wrist line only (not a second thick arm)\n          ctx.strokeStyle = skin;\n          ctx.lineWidth = 5;\n          ctx.lineCap = \"round\";\n          ctx.beginPath();\n          ctx.moveTo(handX + 4, 2);\n          ctx.lineTo(mx, my);\n          ctx.stroke();\n          ctx.fillStyle = skin;\n          rr(ctx, mx - 4, my - 3, 11, 9, 3);\n          if (showMag && anim === \"mag\") {\n            ctx.fillStyle = \"#1a1520\";\n            rr(ctx, mx - 2, my + 4, 7, 13, 1);\n          }\n          if (anim === \"slide\" && rt > 0.55 && rt < 0.9) {\n            // slide rack motion cue\n            const k = (rt - 0.55) / 0.35;\n            ctx.fillStyle = \"#888\";\n            rr(ctx, handX + 8 - k * 6, -6, 10, 4, 1);\n          }\n        } else if (anim === \"pump\") {\n          const pump = rt < 0.45 ? (rt / 0.45) : (1 - (rt - 0.45) / 0.55);\n          const sx = forendX - pump * barrelAhead * 0.38;\n          const sy = forendY;\n          ctx.strokeStyle = skin;\n          ctx.lineWidth = 5;\n          ctx.beginPath();\n          ctx.moveTo(handX + 6, 1);\n          ctx.lineTo(sx, sy);\n          ctx.stroke();\n          ctx.fillStyle = skin;\n          rr(ctx, sx - 2, sy - 3.5, 12, 8, 3);\n        } else if (anim === \"bolt\") {\n          const bolt = rt < 0.4 ? (rt / 0.4) : (1 - (rt - 0.4) / 0.6);\n          const sx = handX + 4 + bolt * 12;\n          const sy = -5 - bolt * 5;\n          ctx.strokeStyle = skin;\n          ctx.lineWidth = 5;\n          ctx.beginPath();\n          ctx.moveTo(handX + 4, 0);\n          ctx.lineTo(sx, sy);\n          ctx.stroke();\n          ctx.fillStyle = skin;\n          rr(ctx, sx, sy - 1, 10, 7, 3);\n        } else if (anim === \"belt\") {\n          const phase = rt;\n          let sx, sy;\n          if (phase < 0";
