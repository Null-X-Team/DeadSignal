window.__DS_EP=window.__DS_EP||[];window.__DS_EP[30]="his.bullets) {\n        b.x += b.vx * dt;\n        b.y += b.vy * dt;\n        b.life -= dt;\n      }\n      this.bullets = this.bullets.filter(\n        (b) => b.life > 0 && b.x > -40 && b.x < WORLD_W + 40 && b.y > -40 && b.y < VIEW_H + 40\n      );\n      for (const b of this.bullets) {\n        for (const e of this.enemies) {\n          if (e.dead) continue;\n          const dx = b.x - e.x;\n          const dy = b.y - e.y;\n          const hit = e.r + b.r;\n          if (dx * dx + dy * dy < hit * hit) {\n            let dmg = b.damage;\n            // armored targets reduce non-piercing shots\n            if ((e.armor || 0) > 0 && !b.pierce) {\n              dmg = Math.max(1, dmg - e.armor);\n            }\n            e.hp -= dmg;\n            e.hit = 0.12;\n            // tall zombies plant their feet \\u2014 no knockback\n            if (e.size !== \"tall\") {\n              e.vx += Math.sign(b.vx) * (b.pierce ? 160 : 110);\n              e.knock = 0.1;\n            }\n            b.life = 0;\n            this.burst(b.x, b.y, b.color, 5, 90);\n            this.bloodHit(b.x, b.y);\n            if (e.hp <= 0) this.kill(e);\n            break;\n          }\n        }\n      }\n      for (const e of this.enemies) {\n        if (e.dead) continue;\n        const dx = this.player.x - e.x;\n        const dist = Math.abs(dx) || 1;\n        e.attack = Math.max(0, e.attack - dt);\n        e.hit = Math.max(0, e.hit - dt);\n        e.knock = Math.max(0, e.knock - dt);\n        const speed = e.speed * (e.knock > 0 ? 0.28 : 1);\n        e.x += (Math.sign(dx) * speed + e.vx) * dt;\n        e.y = FLOOR_Y - (e.tall || 56);\n        e.facing = dx >= 0 ? 1 : -1;\n        e.step += dt * (e.size === \"short\" ? 16.5 : e.size === \"tall\" ? 5.5 : e.type === \"runner\" ? 13 : e.type === \"brute\" ? 7 : 9);\n        e.vx *= Math.pow(1e-3, dt);\n        if (dist < e.r + this.player.r * 0.72 && e.attack <= 0) {\n          this.hurt(e.damage);\n          e.attack = 0.85;\n          // tall zombies plant feet \\u2014 no recoil push either\n       ";
