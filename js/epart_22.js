window.__DS_EP=window.__DS_EP||[];window.__DS_EP[22]="      if (this.wave > 4 && roll > 0.78) type = \"brute\";\n      else if (this.wave > 3 && roll > 0.62) type = \"armored\";\n      else if (this.wave > 2 && roll > 0.55) type = \"runner\";\n      const STATS = {\n        walker:  { hp: 80,  speed: 72,  r: 28, damage: 13, color: \"#7ee8d4\", score: 12, armor: 0 },\n        runner:  { hp: 48,  speed: 128, r: 24, damage: 10, color: \"#c9d2dc\", score: 18, armor: 0 },\n        armored: { hp: 110, speed: 58,  r: 30, damage: 16, color: \"#8a9aaa\", score: 28, armor: 28 },\n        brute:   { hp: 160, speed: 50,  r: 34, damage: 20, color: \"#e35d6a\", score: 40, armor: 18 }\n      };\n      const stats = STATS[type] || STATS.walker;\n      // Size variety (mixed per wave): short = faster / less HP / less dmg;\n      // tall = slower / more HP / more dmg; normal = baseline. Not all same size.\n      const sr = Math.random();\n      let size = \"normal\";\n      if (sr < 0.32) size = \"short\";\n      else if (sr > 0.68) size = \"tall\";\n      // Brutes bias tall; runners bias short (still keep some variety)\n      if (type === \"brute\" && size === \"short\") size = Math.random() < 0.7 ? \"tall\" : \"normal\";\n      if (type === \"runner\" && size === \"tall\") size = Math.random() < 0.7 ? \"short\" : \"normal\";\n      const SIZE = {\n        short:  { scale: 0.70, hpMul: 0.58, spdMul: 1.55, dmgMul: 0.62, tall: 42 },\n        normal: { scale: 1.0,  hpMul: 1.0,  spdMul: 1.0,  dmgMul: 1.0,  tall: 56 },\n        tall:   { scale: 1.42, hpMul: 1.65, spdMul: 0.52, dmgMul: 1.48, tall: 78 }\n      };\n      const sz = SIZE[size] || SIZE.normal;\n      const hp = Math.round((stats.hp + this.wave * 5) * sz.hpMul);\n      const speed = (stats.speed + this.wave * 1.8) * sz.spdMul;\n      const damage = Math.round(stats.damage * sz.dmgMul);\n      const r = Math.round(stats.r * sz.scale);\n      this.enemies.push({\n        id: this.id++,\n        type,\n        size,\n        sizeScale: sz.scale,\n        x: door.x,\n        y: FLOOR_Y - sz.tall,\n        vx: 0,\n        vy: 0,\n        r,\n";
